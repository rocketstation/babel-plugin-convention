export const helper = '$'

export const origin = {
  api: 'api',
  web: 'web',
}

export const originValues = Object.values(origin)

export const source = {
  library: 'lib',
  source: 'src',
}

export const sourceValues = Object.values(source)

export const tag = {
  $audio: 'audio',
  $box: 'div',
  $canvas: 'canvas',
  $editor: 'textarea',
  $form: 'form',
  $frame: 'iframe',
  $header: 'h1',
  $img: 'img',
  $input: 'input',
  $label: 'label',
  $link: 'a',
  $meta: 'meta',
  $option: 'option',
  $path: 'path',
  $script: 'script',
  $select: 'select',
  $shape: 'svg',
  $text: 'span',
  $title: 'title',
  $video: 'video',
}

export const tagValues = Object.values(tag)

export const module = {
  actions: 'actions',
  assets: 'assets',
  component: 'component',
  configs: 'configs',
  hooks: 'hooks',
  locales: 'locales',
  model: 'model',
  root: 'index',
  segments: 'segments',
  services: 'services',
  skins: 'skins',
}

export const moduleValues = Object.values(module)
